node_modules/
dist/
.DS_Store
*.local
.env

# Raw .Stat Explorer exports -- regenerate any time via the links in
# README.md -> "Data Sources". A few MB each and several are byte-
# identical whole-dataflow dumps, not worth carrying in git history.
data-pipeline/raw/*.csv

# Generated from src/styles/tokens.js by scripts/build-tokens.mjs, and
# rewritten by Vite before every build and dev server start. Committing it
# would mean a second place for the palette to be wrong.
src/styles/tokens.css
